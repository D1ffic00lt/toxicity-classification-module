[tox]
envlist = py{37,38,39,310,311}

minversion = 3.3.0

isolated_build = true

[testenv]
deps =
    check-manifest >= 0.42
    flake8
commands =
    python setup.py check -m -s
    flake8 .

[flake8]
exclude = .tox,*.egg,build,data
select = E,W,F